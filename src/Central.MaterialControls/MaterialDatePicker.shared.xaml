<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Central.MaterialControls"
             x:Class="Central.MaterialControls.MaterialDatePicker">
    <ContentView.Content>
        <Grid ColumnSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="1"/>
            </Grid.RowDefinitions>
            <!--<BoxView x:Name="BackBox" CornerRadius="5,5,0,0" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" BackgroundColor="WhiteSmoke" Grid.RowSpan="2"/>-->
            <views:BorderlessDatePicker Opacity="0" x:Name="Picker" Margin="10,12,0,0"/>
            <Label x:Name="HiddenLabel" FontSize="10" IsVisible="False" Margin="10,0,0,0"/>
            <views:BorderlessEntry x:Name="EntryField" Text="{Binding Text, Mode=TwoWay}" Margin="10,12,0,0"/>
            <BoxView x:Name="BottomBorder" BackgroundColor="Gray" Grid.Row="1" HeightRequest="1" Margin="0" HorizontalOptions="FillAndExpand"/>
            <BoxView x:Name="HiddenBottomBorder" BackgroundColor="Gray" Grid.Row="1" HeightRequest="1" Margin="0" WidthRequest="0" HorizontalOptions="Center"/>
        </Grid>
    </ContentView.Content>
</ContentView>